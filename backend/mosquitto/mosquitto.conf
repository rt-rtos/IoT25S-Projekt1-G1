# Development broker config. Mosquitto 2.x binds to localhost only unless
# a listener is declared, so this file is required for the node to connect.
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type error
log_type warning
log_type notice
